apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpresschart.fullname" . }}-test-connection"
  labels:
{{ include "wordpresschart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "wordpresschart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
